/* Eliminate the comment by fading it out */
  $j('#comment_<%= @comment.id %>').fadeOut();
  /* Replace the count of comments */
  $j("#comments_count").html("<%= pluralize(Comment.count, 'comments') %>");
 
